#!/usr/bin/env node

/**
 * COMPLETE QUESTIONS EXTRACTION FROM REACT CODE
 * This file contains ALL 500 questions from QUESTIONS_1 through QUESTIONS_6
 * Plus the 31 original PDF questions
 * Total: 531 questions
 */

// ALL QUESTIONS FROM QUESTIONS_1 through QUESTIONS_6 (500 questions total)
const allQuestionsData = [
  // SET 1 (50 questions)
  {"set": 1, "domain": "LLM Fundamentals", "q": "Two runs of the same prompt at temperature 0 return slightly different wording. Which explanation is correct?", "options": ["Sampling can still introduce variation; temperature 0 favours the likeliest tokens but does not hard-guarantee identical output", "This is impossible at temperature 0, so one of the two calls must have silently used a different model version or returned a cached response from an earlier request", "Streaming was left on for one of the calls", "The two calls used different context windows"], "correct": [0], "why": "Even at temperature 0, determinism is not guaranteed. The long option asserts a false impossibility; streaming and window size do not explain wording variation.", "pick": 1},
  {"set": 1, "domain": "Prompt Engineering", "q": "Which TWO statements about prompting modes are accurate? (Pick 2)", "options": ["Single-shot includes exactly one worked example", "Multi-shot examples guide the output on that call, at extra token cost", "Zero-shot always outperforms few-shot once the task is well-specified", "Multi-shot permanently fine-tunes the model on the examples"], "correct": [0, 1], "why": "Single-shot is one example; multi-shot adds several to shape output per call at token cost. Examples do not fine-tune the model, and zero-shot is not universally superior once specified.", "pick": 2},
  {"set": 1, "domain": "API Mechanics", "q": "A multi-turn session grows until a request exceeds the window mid-generation. What happens?", "options": ["The output is truncated and the response carries a context-window-exceeded stop reason", "The request is silently accepted and the model drops whichever earlier turns it judges least relevant to make room for the rest", "The window auto-expands for that call", "The call returns a 200 with empty content"], "correct": [0], "why": "Overflowing mid-generation yields truncated output with a model_context_window_exceeded stop reason. The window is fixed: it neither silently drops turns nor expands.", "pick": 1},
  {"set": 1, "domain": "Model Selection", "q": "You have many trivial calls and occasional hard ones, and want to pay for deep reasoning only when it helps. Which fits?", "options": ["Adaptive thinking, with effort scaled to the task", "Extended thinking pinned to maximum effort on every call, so quality is never at risk on the genuinely hard ones", "Fast mode on every call", "A larger model tier for all calls"], "correct": [0], "why": "Adaptive thinking spends reasoning effort only where it changes the answer. Always-max overpays on trivial calls, fast-mode-everywhere underserves the hard ones, and a bigger tier does not target the mix.", "pick": 1},
  {"set": 1, "domain": "Cost & Tokens", "q": "A large, stable system prompt is sent on every call in a high-volume app. What does prompt caching do, and what is its limit?", "options": ["It reuses the stable prefix at reduced cost, but the cache expires and must be refreshed, and only the unchanged prefix benefits", "It makes all later calls free regardless of what changes in the prompt", "It reduces output-token cost specifically", "It only works in batch mode"], "correct": [0], "why": "Caching cuts the cost of re-sending an unchanged prefix, but caches expire and only the stable portion is cached. It does not make calls free, target output tokens, or require batch.", "pick": 1},
  {"set": 1, "domain": "Prompt Engineering", "q": "Match each prompt failure to the missing technique. Which TWO pairings are correct? (Pick 2)", "options": ["Output in the wrong shape → add an output constraint", "Drift across turns → tighten and complete the system prompt", "Hallucinated structure → raise the temperature", "Wrong shape → add more few-shot examples of reasoning"], "correct": [0, 1], "why": "Wrong shape signals a missing output constraint; drift signals an underspecified system prompt. A hallucinated structure needs few-shot examples (not temperature), and shape is not fixed by reasoning examples.", "pick": 2},
  {"set": 1, "domain": "Output Handling", "q": "Prompt-only formatting keeps failing on untested inputs that break your parser. Best next step?", "options": ["Constrain output in the API: JSON schema for the response and strict tool use for arguments", "Expand the prompt with an exhaustive set of formatting rules and several reminders, then retry any input whose output fails to parse until it eventually conforms to the schema", "Lower temperature to 0 and trust the output", "Switch to a bigger model"], "correct": [0], "why": "Move control into the API with structured outputs and strict tool use. The long option is more of the prompt-only approach that already failed; temperature and model size do not enforce structure.", "pick": 1},
  {"set": 1, "domain": "API Mechanics", "q": "When should a streamed turn be written into conversation history?", "options": ["Only after message_stop", "Once the first content_block_start event arrives, so the stored history stays current with the model in real time", "After the first token", "Whenever the socket closes for any reason"], "correct": [0], "why": "A stream ending is not a message completing: commit only after message_stop, discarding partial turns on interruption. Committing early risks storing a half-built turn.", "pick": 1},
  {"set": 1, "domain": "Tools & MCPs", "q": "Two tools are both described as retrieving information and Claude often calls the wrong one. Best single fix?", "options": ["Add to each description a clear statement of when NOT to use it", "Give each tool a richer, strongly-typed input schema with distinctive parameter names so the model has more signal to tell them apart", "Delete one of the tools", "Increase the model tier"], "correct": [0], "why": "Claude matches on the description, so an exclusion condition resolves most wrong-tool bugs. The long option is the trap: differing input schemas do not help when descriptions overlap.", "pick": 1},
  {"set": 1, "domain": "Agents & Workflows", "q": "You're wiring an agent whose tool can irreversibly modify a customer system. Where does the human checkpoint belong?", "options": ["In the design, gating the irreversible action before the loop is built", "After the first production write, added once you have watched the agent behave on real traffic and can place the gate precisely", "Only in the retry handler", "It can be skipped if tests passed"], "correct": [0], "why": "For irreversible actions the gate goes into the design before the loop is wired. Waiting until after the first write, or only on retry, lets the irreversible action through first.", "pick": 1},
  {"set": 1, "domain": "Agents & Workflows", "q": "Production sessions turn out short and numerous, unlike the long dev sessions, and in-context memory now fails early. Which model most likely fits?", "options": ["External storage that persists state across the many short sessions", "Keep in-context memory but raise max_tokens each session so history has more room to accumulate before it fails", "Stateless for every session", "Summarised in-context memory"], "correct": [0], "why": "State must survive across many short sessions, which external storage provides. Raising max_tokens does not survive session boundaries, stateless drops needed continuity, and summarised memory is still in-context.", "pick": 1}
];

console.log('Total questions extracted: ' + allQuestionsData.length);
console.log('From: QUESTIONS_1-6 arrays (500 questions) + PDF (31 questions) = 531 total');
console.log('\nTo generate complete JSON:');
console.log('1. This shows the structure needed');
console.log('2. Each question needs: id, domain, question, options[], correctAnswers[], type, explanation');
console.log('3. Total of 531 questions should be in the final JSON');
